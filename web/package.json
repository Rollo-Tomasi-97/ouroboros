{
  "name": "ouroboros-web",
  "version": "6.96.3",
  "private": true,
  "type": "module",
  "description": "Ouroboros browser UI package boundary",
  "scripts": {
    "lint": "node --check app.js",
    "test": "node --test tests/*.test.js"
  }
}
